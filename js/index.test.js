/**
 * @jest-environment jsdom
 */

const index = require('./index');

describe('Render index page', () => {
  test(
      'Shows correct format message if the address text input is valid',
      () => {});

  test(
      'Shows an error message if the address text input is invalid',
      async () => {});
});